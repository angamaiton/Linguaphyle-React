import React from 'react';

const NotFound = () => (
  <div className="row">
    <div className="col">
      <h3>Oh no!</h3>
      <p>You've hit a page that doesn't exist (yet) -- we call that a 404.</p>
    </div>
  </div>
);

export default NotFound;
